# p01_gcd 子 Makefile（AM 风格：判分核心见 ../../../core，本语言钩子见 ../../lang.mk）
#
# 用法：在本目录或根目录执行 make sim 判分。
# 模板即 gcd.c：直接在其中 TODO 区填写实现，请勿改动文件名与函数签名（见 gcd.h）；
# test/ 目录只放判分端 harness，不随题目发放；本 Makefile 请勿改动。

PID    := p01_gcd
MODULE := gcd
MODE   := func

LANG := ../..

include $(LANG)/lang.mk
