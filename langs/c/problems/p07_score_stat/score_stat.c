// p07_score_stat 成绩统计（C 程序设计·标准输入输出 + 排序）
//
// 本题为 stdin/stdout 对拍题：你要自己写 main，从标准输入读数据、往标准输出打结果。
//
// 输入格式：
//   第一行：整数 n（1 <= n <= 100000）
//   第二行：n 个整数，用空格分隔，每个是 0..100 的成绩
// 输出格式（共三行）：
//   第一行：最高分 最低分（空格分隔）
//   第二行：平均分，保留两位小数（printf 的 "%.2f"）
//   第三行：n 个成绩按从大到小排序，用单个空格分隔（行尾不要多余空格）
//
// 样例输入：           样例输出：
//   5                    91 60
//   78 91 60 85 78       78.40
//                        91 85 78 78 60
//
// 提示：n 可以到 1e5，冒泡排序会超时（本题单次运行上限见子 Makefile 的 TIMEOUT）。
//       用计数排序（成绩只有 0..100 共 101 种取值）或 O(n log n) 排序。
//       判分开启了 AddressSanitizer，数组开小了会直接判 RE。
#include <stdio.h>

// ===== TODO: 在此完成你的设计 =====

int main(void)
{
    return 0;
}

// ===== END =====
